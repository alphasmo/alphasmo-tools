// Kept in sync by hand with package.json's "version" field — a runtime package.json read/import
// is unreliable across this package's ESM/CJS dual build and older supported Node versions.
export const VERSION = "0.2.0";
